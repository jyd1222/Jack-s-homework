#symbol of type String
#maxnum,num_symbols,num_space,i of type Integer

symbol=input('input a symbol: ')
maxnum=int(input('input max number of symbol(odd): '))

while maxnum%2!=1:
    maxnum=int(input('input max number of symbol(odd): '))
num_symbol=1
num_space=(maxnum-1)//2
   
while num_symbol<=maxnum:
    
    for i in range(num_space):
        print(' ',end='')
    for i in range(num_symbol):
        print(symbol,end='')
    print('')
    num_space-=1
    num_symbol+=2




symbol=input('input a symbol: ')
maxnum=int(input('input max number of symbol(odd): '))
num_space=maxnum//2
space=' '*num_space
print(space+symbol)
mid=1
num_space-=1
for i in range(maxnum//2-1):
    
    print(' '*num_space+symbol+mid*' '+symbol)
    mid+=2
    num_space-=1
   

print(symbol*maxnum)
